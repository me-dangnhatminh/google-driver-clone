export * from './storage.service';

import { StorageService } from './storage.service';

export const services = [StorageService];

export const providers = [StorageService];
export default providers;
